name: Lint & Format & Test

on:
  push:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 8
          run_install: true
      - name: lint
        run: |
          pnpm biome ci .
